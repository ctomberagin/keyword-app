import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import UserDiv from './App';

ReactDOM.render(
  <React.StrictMode>
    <UserDiv />
  </React.StrictMode>,
  document.getElementById('root')
);
